/* recursion: 720 in the bss address */
#include <stdio.h>
int x;

int fatorial (int n)
{
    int temp;
    if (n == 1) {
       return n;
    };
    temp = fatorial(n-1) * n;
    printf("%d\n", temp);
    return temp;          
}

int main()
{
  int y;
  y = 6;
  x = fatorial(y);
  return 0;
}
